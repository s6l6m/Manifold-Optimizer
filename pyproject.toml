[project]
name = "manifold-optimizer"
version = "0.1.0"
requires-python = "==3.11.*"
dynamic = ["dependencies"]

[tool.setuptools]
packages = []

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}
